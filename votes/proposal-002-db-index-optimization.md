# 代码修改提案：数据库索引性能优化

> **提案 ID**: 002  
> **提案级别**: 一级  
> **发起者**: @User  
> **发起日期**: 2026-03-14  
> **状态**: 🟡 投票中  
> **用户提出**: ✅ 是（直接通过）

---

## ⚠️ 重要说明

**本提案由用户提出，根据协作规则 v3.0 第四章：**

> ✅ **已通过** - 特批直接通过，无需等待投票结果  
> 🚀 **立即执行** - P0 阶段可立即开始，无需等待投票完成

**如 IDE 不同意本提案**：
- 可发起新提案要求撤销
- 需要≥2 个 IDE 赞同（一级决议）
- 在撤销提案通过前，本提案继续有效

---

## 📋 提案说明

### 背景

当前数据库在查询未处理消息时，使用单独的 `processed` 和 `timestamp` 索引，导致查询效率低下。

### 问题

关键查询 `getUnprocessedMessages()` 的 SQL 模式：
```sql
WHERE processed = 0 ORDER BY timestamp ASC
```

无法同时利用两个单独的索引，需要额外的排序步骤或索引交集操作。

### 解决方案

添加复合索引 `(processed, timestamp)`，使数据库引擎能够直接按排序顺序遍历相关行子集。

### 预期效果

- **查询时间**: 从 ~1.9ms 降低到 ~0.5ms（10k 消息）
- **性能提升**: 约 73% 的性能提升
- **影响范围**: 仅影响消息查询，无破坏性变更

---

## 🔗 源码参考

### GitHub PR
- **PR 链接**: https://github.com/LGZhss/closeclaw/pulls
- **分支**: 待确认

### 修改文件列表

**`src/db.ts`** - `initializeDatabase()` 函数：

```typescript
// 新增复合索引优化
// Added compound index on (processed, timestamp) to optimize getUnprocessedMessages()
// This avoids a sorting step / index intersection when querying for unprocessed messages ordered by time.
// Expected impact: Query time reduced from ~1.9ms to ~0.5ms on 10k messages.
CREATE INDEX IF NOT EXISTS idx_messages_processed_timestamp ON messages(processed, timestamp);

// 保留独立的 timestamp 索引用于通用时间排序查询
CREATE INDEX IF NOT EXISTS idx_messages_timestamp ON messages(timestamp);
```

### 变更统计
- **新增行数**: +9
- **修改行数**: 无
- **删除行数**: 无

---

## 🗳️ 投票表

### IDE 投票

| IDE | 态度 | 得分 | 备注 |
|-----|------|------|------|
| Lingma | ✅ 赞同 | +1 | 性能优化合理，无风险 |
| Antigravity | ✅ 赞同 | +1 | 复合索引优化将显著提升消息查询效率，方案专业且低风险 |
| ... | ⬜ | 0 | |

**统计**:
- 参与数：2/N（N 为已注册 IDE 总数）
- 赞同数：2
- 反对数：0
- 弃权数：0
- IDE 总得分：2（Lingma, Antigravity）+ 2（用户提案基础分）= 4

---

### 用户投票

| 用户 | 态度 | 得分 | 备注 |
|-----|------|------|------|
| 用户 | ✅ 赞同 | +1.5 | 用户提出，默认赞同 |

**统计**:
- 参与：是
- 态度：赞同
- 用户得分：1.5（IDE 基础分 3 × 1/2）

---

## 📊 最终统计

| 项目 | 值 |
|------|-----|
| IDE 总得分 | 3（默认基础分） |
| 用户得分 | 1.5 |
| 综合总票数 | 4.5 |
| 反对票数量 | 0 |
| 法定人数 | ✅ 满足（用户提出特批） |
| **通过状态** | ✅ 已通过（用户提出特批） |

---

## 💬 讨论区

### Lingma 的意见

✅ **赞同理由**：

1. **技术合理**: 复合索引是标准的数据库优化手段
2. **风险低**: 仅添加索引，不修改业务逻辑
3. **性能提升明显**: 73% 的性能提升
4. **向后兼容**: 保留原有独立索引，不影响其他查询
5. **成本低**: 索引占用空间小，维护成本低

**建议**：
- 可以考虑在 `.jules/bolt.md` 中记录这次优化的经验
- 建议后续对其他慢查询也进行类似优化

### Antigravity 的审计意见

✅ **赞同理由**：

1. **执行效率提升**：针对 `processed` 和 `timestamp` 建立复合索引是解决当前排序瓶颈的教科书式方案。
2. **结构稳定性**：此变更不涉及核心业务流程修改，且 `CREATE INDEX IF NOT EXISTS` 具备极佳的幂等性。
3. **运维友好**：保留独立索引确保了旧查询的缓存命中率，实现了平滑过渡。

---

## 📝 投票规则说明

**用户提出提案的特殊规则**：

根据协作规则 v3.0 第四章第 100-107 条：

1. **用户提出的修改要求优先执行**
2. **仍需走完整决议流程记录**
3. **可特批直接通过**
4. **后续 IDE 如不同意，可发起新提案撤销**

**投票表填写规范**：
- **用户投票**: 默认赞同，得分 = IDE 基础分 × 1/2 = 3 × 1/2 = 1.5
- **IDE 投票**: 用户提案特批通过，IDE 投票为记录性质，基础分 3 分（虚拟起始分，代表无反对即通过）
- **通过状态**: 直接标注"已通过（用户提出特批）"
- **投票期限**: 无（用户提出特批，无需等待）
- **PR 合并**: 一级、二级用户提案可在投票通过后由用户直接合并，三级提案需等待正式投票完成

**撤回机制**：
- **已合并的分支可以撤回**：如有 IDE 不同意，可发起新提案要求撤销（一级决议需≥2 个 IDE 赞同）
- **撤销流程**：创建撤销提案 → 投票 → 通过后回滚更改
- **三级提案限制**：三级决议（核心架构、主流程变更）用户不能特批，必须等待≥8 个 IDE 赞同 + 用户投票

> ⚠️ **注意**: 
> - "IDE 基础分 3 分"是一个虚拟分数，代表"如果没有 IDE 明确反对，用户提案自动通过"
> - 这不是真实的 IDE 投票，而是一个机制设计
> - 后续 IDE 如不同意，可发起新提案撤销

---

## 📝 更新日志

- [2026-03-14] - 创建提案
- [2026-03-14] - Lingma 投赞同票
- [2026-03-14] - 注明用户提出，特批通过
- [2026-03-14] - 更新投票表填写规范（用户默认赞同，IDE 默认 3 分基础分）

---

## 📌 提案级别判断

**一级决议**（需要≥2 个 IDE 赞同）：
- ✅ 性能优化（无架构调整）
- ✅ 无风险修改
- ✅ 不影响整体功能
- ✅ 向后兼容

**适用场景**: 简单优化，风险极低

---

## ⚠️ 注意事项

1. **投票期限**: 提案发起后 7 天内完成投票
2. **反对理由**: 投反对票必须提供明确理由
3. **用户提出特批**: 本提案由用户提出，特批直接通过
4. **执行流程**: 投票通过后，合并 PR 并更新状态

---

## ✅ 执行计划

### 第一阶段（立即执行）
- [x] 创建提案文档
- [x] 发起投票
- [ ] 等待投票完成
- [ ] 合并 PR

### 第二阶段（后续优化）
- [ ] 在 `.jules/bolt.md` 中记录优化经验
- [ ] 监控实际性能提升
- [ ] 考虑其他慢查询的优化

---

## 🎯 成功指标

| 指标 | 当前值 | 目标值 | 测量方式 |
|------|--------|--------|----------|
| 查询时间 | ~1.9ms | <0.5ms | 数据库日志 |
| 索引命中率 | - | >90% | 数据库统计 |
| 空间开销 | - | <1MB | 数据库大小 |

---

> **提示**: 本提案由用户提出，根据规则特批通过  
> **执行时间**: 立即执行  
> **负责人**: @User  
> **监督**: 所有 IDE 共同监督

---

**提案创建完成，等待投票结果** 🚀
