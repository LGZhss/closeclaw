# 提案：重构 safeCmd 以防止命令注入

> **状态**: ✅ 已通过（用户特批）

## 问题

`safeCmd` 实用函数使用 `execSync`，它通过 shell 解释器执行命令。如果传递了未经过滤的用户输入，这容易导致命令注入攻击。

## 解决方案

重构 `safeCmd` 以使用 `execFileSync` 并设置 `shell: false`。这要求将可执行文件及其参数分开传递，从而防止 shell 元字符的解释，彻底消除命令注入的风险。同时将错误处理块中的 `any` 替换为 `unknown`，以提高类型安全性。
