name: Multi-Remote Sync

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  sync-mirrors:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Mirror to Gitee
<<<<<<< HEAD
        uses: Yikun/hub-mirror-action@master
        with:
          src: github/LGZhss
          dst: gitee/lgzhss
          static_list: "closeclaw"
          dst_key: ${{ secrets.GITEE_PRIVATE_KEY }}
          dst_token: ${{ secrets.GITEE_TOKEN }}
          account_type: user
          force_update: true
        if: always()

      - name: Mirror to Codeup (Alibaba)
        run: |
          git remote add codeup https://codeup.aliyun.com/69c15eb9ad0a337b92d8feaa/LGZhss/closeclaw.git
          git push -f codeup main
        env:
          CODEUP_USER: ${{ secrets.CODEUP_USER }}
          CODEUP_TOKEN: ${{ secrets.CODEUP_TOKEN }}
        if: always()

      - name: Mirror to JihuLab (GitLab)
        run: |
          git remote add gitlab https://oauth2:${{ secrets.GITLAB_TOKEN }}@jihulab.com/lgzhsss/closeclaw.git
          git push -f gitlab main
        if: always()
=======
        run: |
          git remote add gitee https://lgzhss:${{ secrets.GITEE_TOKEN }}@gitee.com/lgzhss/closeclaw.git
          git push -f gitee main

      - name: Mirror to Codeup (Alibaba)
        run: |
          git remote add codeup https://${{ secrets.CODEUP_USER }}:${{ secrets.CODEUP_TOKEN }}@codeup.aliyun.com/69c15eb9ad0a337b92d8feaa/LGZhss/closeclaw.git
          git push -f codeup main

      - name: Mirror to JihuLab
        run: |
          # JihuLab might need JIHU_TOKEN in secrets
          git remote add jihulab https://LGZhss:${{ secrets.JIHU_TOKEN }}@jihulab.com/LGZhss/closeclaw.git
          git push -f jihulab main
>>>>>>> bc72c788d0e4b051a2da245bab01cb5949fc16aa
