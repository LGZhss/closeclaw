name: CodeScene

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  codescene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # The codescene-io/codescene-ci-cd repository has been removed from GitHub.
      # The previous empear-analytics namespace is deprecated.
      # Temporarily commenting out until the new official action is provided.
      # - name: CodeScene Analysis
      #   uses: codescene-io/codescene-ci-cd@v2
      #   continue-on-error: true
      #   with:
      #     codescene-enterprise-token: ${{ secrets.CODESCENE_ENTERPRISE_TOKEN }}
